<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.config.compare.mapper.ConfigCategoryMapper">
    
    <!-- 基础表名：config_category -->

    <!-- 根据父分类ID查询子分类列表 -->
    <select id="selectByParentId" resultType="com.config.compare.entity.ConfigCategory" databaseId="oracle">
        SELECT id,
               category_name,
               category_code,
               parent_id,
               status,
               sort_order,
               description,
               create_time,
               update_time,
               create_by,
               update_by
        FROM config_category
        WHERE parent_id = #{parentId}
        ORDER BY sort_order ASC, category_name ASC
    </select>

    <!-- 查询所有启用的配置分类（用于构建树） -->
    <select id="selectEnabledCategories" resultType="com.config.compare.entity.ConfigCategory" databaseId="oracle">
        SELECT id,
               category_name,
               category_code,
               parent_id,
               status,
               sort_order,
               description,
               create_time,
               update_time,
               create_by,
               update_by
        FROM config_category
        WHERE status = 1
        ORDER BY sort_order ASC, category_name ASC
    </select>

    <!-- 根据服务器类型ID查询适用的配置分类列表 -->
    <select id="selectByServerTypeId" parameterType="long" resultType="com.config.compare.entity.ConfigCategory" databaseId="oracle">
        SELECT id,
               category_name,
               category_code,
               parent_id,
               status,
               sort_order,
               description,
               create_time,
               update_time,
               create_by,
               update_by
        FROM config_category
        WHERE status = 1
        ORDER BY sort_order ASC, category_name ASC
    </select>

</mapper>