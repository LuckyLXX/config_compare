package com.config.compare.config;

/**
 * Swagger配置类 - 临时注释
 * 
 * @author system
 * @version 1.0.0
 * @since 2025-01-25
 */
/*
@Configuration
@EnableOpenApi
public class SwaggerConfig {

    @Bean
    public Docket createRestApi() {
        return new Docket(DocumentationType.OAS_30)
                .apiInfo(apiInfo())
                .select()
                .apis(RequestHandlerSelectors.basePackage("com.config.compare.controller"))
                .paths(PathSelectors.any())
                .build();
    }

    private ApiInfo apiInfo() {
        return new ApiInfoBuilder()
                .title("配置比对系统API文档")
                .description("UAT生产环境配置比对系统RESTful API接口文档")
                .version("1.0.0")
                .contact(new Contact("Config Compare Team", "", "team@example.com"))
                .build();
    }
}
*/